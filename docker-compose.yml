version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: scuffed-uno
    container_name: scuffed-uno-app
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
